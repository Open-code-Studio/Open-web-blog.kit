## 2026-06-15

- 创建了 Open-web-blog.kit 博客站点，基于 JMCL 项目的 MD3 架构。
- 建立完整文件结构：根引导页 → main/（MD3 UI + CSS/JS）→ page/（Markdown 文章）。
- 第一篇博客 `page/hi!.md` 已创建，包含 Markdown 示例内容。
- 使用 config.json 驱动导航和文章路由，支持深色/浅色主题切换、代码高亮、响应式布局、自动目录生成。

- 重构为双视图架构：首页视图（Hero 区域 + 文章卡片列表含摘要/日期/标签）+ 文章详情视图。
- 新增 `main/css/home.css` 样式文件，包含渐变色 Hero、文章卡片悬浮动画、空状态占位。
- 品牌副标题翻译为"博客"，文章列表标注"文章列表"。

- 文章卡片改为 CSS Grid 响应式布局：≤768px 单列，769-1300px 双列，≥1300px 三列。

- 新增横向组件：主题分类卡片行（topic-cards）、标签筛选栏（tag-pills），支持点击筛选文章列表。
- 标签筛选逻辑：根据文章 tags 自动生成筛选按钮，按文章数量排序，筛选后更新文章标题和计数。

- 拆分为多页面架构：index.html(首页)、page.html(文章+侧边栏)、link.html(友链)、about.html(关于)。

- Hero 区域改为圆角卡片框（rounded card with background + border + shadow）。
- 主题默认色改为 #4AA26F（绿色），CSS 和 config.json 均以此值作为默认。
- 主题色支持从 config.json 的 `site.themeColor` 动态配置，JS 自动计算明/暗衍生色并应用到 CSS 变量。

- Footer "Built with ❤️" 移到页面最底部（sticky footer），使用 flex 布局推到底部。
- About 页面内容改为从 config.json `about` 字段配置，支持 sections（title + text/tags/links）。

- Link 页面改为从 config.json `links` 字段配置，支持 title/description/items（name/desc/url/icon），自动渲染链接卡片。
- 新增圆角浮动导航栏(`nav.css`)，pill形独立图层，position:fixed居中，毛玻璃效果，链接四个子页面。
- 侧边栏仅在 page.html 显示，其他页面使用 `.main-content--full` 全宽布局。
- 首页文章卡片点击跳转 `page.html#/{route}`，page.html 通过 hash 路由加载文章。
